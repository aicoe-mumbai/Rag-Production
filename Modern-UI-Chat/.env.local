DB_HOST=sql12.freesqldatabase.com
DB_NAME=sql12776989
DB_USER=sql12776989
DB_PASSWORD=5vACdqgBj2
DB_PORT=3306

# Internal backend URLs (not exposed to client)
# These are used by the Next.js API proxy
DJANGO_API_URL=http://localhost:8000
PYTHON_API_URL=http://localhost:8444
TGI_SERVER_URL=http://localhost:8080

# Public API URL for client-side requests
# This should point to the Next.js API proxy
# In both development and production, this will be the same domain as the frontend
NEXT_PUBLIC_API_URL=/api/proxy